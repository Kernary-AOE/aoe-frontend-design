<atom
  id="@community/pattern-blue-green-deploy"
  kind="knowledge"
  version="1.0.0"
  domain="infrastructure"
  tokens="740"
  q="4.0">
  <description>Run two identical production environments (blue and green); only one serves live traffic at a time. Deploy new code to the idle environment, run smoke tests, then atomically switch the traffic router to the new environment. Roll back by switching the router back.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="81"/>
    <level name="core"    file="chunks/core.md"    tokens="740"/>
    <level name="full"    file="chunks/full.md"    tokens="1563"/>
  </projection>
</atom>
