<atom
  id="@community/principle-idempotent-writes"
  kind="knowledge"
  version="1.0.0"
  domain="data-engineering"
  tokens="474"
  q="4.0">
  <description>Every write to a downstream system (database, queue, API, blob store) must be safely retryable: re-applying the same operation N times must produce exactly the same observable state as applying it once. In distributed systems, retries are inevitable — at-least-once delivery is the default.</description>
  <projection>
    <level name="summary" file="chunks/summary.md" tokens="192"/>
    <level name="core"    file="chunks/core.md"    tokens="474"/>
    <level name="full"    file="chunks/full.md"    tokens="788"/>
  </projection>
</atom>
