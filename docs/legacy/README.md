# Legacy Frontend Design documentation

These files describe the pre-Kernary 899-Atom corpus, old `primes-v3` paths,
five-tool MCP wrapper, and hand-composed retrieval pipeline. They are preserved
for migration evidence and historical benchmarks.

Current documentation starts at [`../overview.md`](../overview.md). Do not use a
legacy count, path, tool surface, or fixed-kind statement as the current Domain
Package contract.
